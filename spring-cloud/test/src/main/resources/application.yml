server:
  port: 8081

spring:
  application:
    name: test-svc

eureka:
  client:
    #eureka 서버에 등록 가능 설정
    register-with-eureka: true
    #외부에서 검색 가능한 형태로 만듬
    fetch-registry: true
    #eureka 엔드포인트에 현재 마이크로서비스를 등록
    service-url:
      defaultZone: http://localhost:8761/eureka
